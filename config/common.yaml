rate: 50
# Note: Comment out to disable move_group-related presets
# move_group_name: arm
timeout:
  press: 0.1
  sequence: 0.2

presets:
  switch_twist_mode:
    trigger: [[R1, R2, L1, L2], 1]
    action:
      - type: set_shared
        key: twist_mode
        value: ['body', 'arm']
  super:
    # Dummy
    trigger: [C_U, C_U, C_D, C_D, C_L, C_R, C_L, C_R, X, O]
    action:
      - type: publish_float64
        topic: foo
        value: 42
